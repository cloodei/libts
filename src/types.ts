export type UserPayload = {
  name: string;
  email: string;
  password: string;
}

export type BookPayload = {
  title: string;
  author: string;
  content: string;
  category: string;
  publish_date: string;
}
